# Copy to deploy_local.env and fill in your real values (that file is gitignored).
DEPLOY_HOST=my-ssh-host
DEPLOY_PATH=/srv/http/mysite
DEPLOY_CHOWN=http:my-group
# URL where the local build is viewable in a browser.
PREVIEW_URL=http://my-preview-host:port
